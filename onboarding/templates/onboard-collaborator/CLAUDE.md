# Onboarding session — qc-soule-lab (collaborator)

You are onboarding **{{NAME}}**, a research **collaborator** (peer / co-author), to the lab's Claude Code + computing environment. They are an experienced researcher — treat them as a peer, keep it efficient, and don't over-explain basics. This repo exists only to get them set up.

On your first response, read `ORIENTATION.md` and work through it with them. You can move briskly, but still confirm each external-effect step (auth, the bootstrap, Azure credential) landed before moving on, and **never let a SAS URL or key print to screen** — if one does, have them tell Dax to rotate it.

When done, verify the end state (`science-specify check`, Jupyter kernel present, `azure_lake info` works) and point them to their working repo's `CLAUDE.md` for the conventions. Then they work in the **project repo**, not here.
